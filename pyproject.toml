[project]
name = "create-litestar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "httpx>=0.27.2",
    "pytest>=8.3.3",
    "questionary>=2.0.1",
    "rich>=13.9.1",
    "ruff>=0.6.8",
]

[tool.ruff]
extend-exclude = ["src/templates/basic"]