[project]
name = "create-litestar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "questionary>=2.0.1",
    "rich>=13.9.1",
    "ruff>=0.6.8",
]

[tool.uv.workspace]
members = ["create-litestar"]
